# Disabled legacy deploy workflow (replaced by gh-pages.yml)
name: (disabled) Deploy to GitHub Pages

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is disabled. Use gh-pages.yml."